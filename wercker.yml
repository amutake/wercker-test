box: wercker/nodejs
build:
  steps:
    - script:
        name: env
        code: env
  after-steps:
    - script:
        name: env
        code: env
deploy:
  steps:
    - script:
        name: env
        code: env
  after-steps:
    - script:
        name: env
        code: env
