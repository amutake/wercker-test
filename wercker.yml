box: wercker/default
build:
  steps:
    - script:
        name: build steps env
        code: |
          env | grep WERCKER
  after-steps:
    - script:
        name: build after-steps env
        code: |
          env
          echo success
deploy:
  steps:
    - script:
        name: deploy steps env
        code: |
          env
          exit 0
  after-steps:
    - script:
        name: deploy after-steps env
        code: env || true
