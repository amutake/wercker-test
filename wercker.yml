box: wercker/default
build:
  steps:
    - script:
        name: build steps env
        code: env || true
  after-steps:
    - script:
        name: build after-steps env
        code: env || true
deploy:
  steps:
    - script:
        name: deploy steps env
        code: env || true
  after-steps:
    - script:
        name: deploy after-steps env
        code: env || true
